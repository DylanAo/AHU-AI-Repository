#pragma once
#include"phone_dir.h"
#include <iostream>
#include <Windows.h>
using namespace std;

void home();
void wrong_choice();
void switch_mode(int mode);
void BIOS(phone_dir& dir, string name);