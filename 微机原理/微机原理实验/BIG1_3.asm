DATA SEGMENT
    X DW 125AH
    Y DW 204CH
    BUFF DW ? 
DATA ENDS

CODE SEGMENT
    ASSUME CS:CODE,DS:DATA

START:
    MOV AX,DATA
    MOV DS,AX
    XOR AX,AX

    MOV AX,X
    MOV BX,Y
    CMP AX,BX
    JB BELOW

    SUB AX,BX
BELOW:
    MOV BUFF,AX

    MOV AH,4CH
    INT 21H
CODE ENDS
END START   