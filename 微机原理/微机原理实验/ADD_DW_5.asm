DATA SEGMENT
    ARRY DW 30 DUP(2)
DATA ENDS

CODE SEGMENT
    ASSUME CS:CODE,DS:DATA

START:
    MOV AX,DATA
    MOV DS,AX
    XOR AX,AX
    MOV CX,30

    LEA BX,ARRY
    ADD BX,58

AGAIN:
    ADD AX, WORD PTR [BX]
    SUB BX,2
    LOOP AGAIN

    MOV AH,4CH
    INT 21H
CODE ENDS
END START