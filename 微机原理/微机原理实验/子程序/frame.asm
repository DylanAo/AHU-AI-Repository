;这里是一个框架，每次用的时候可以把这个复制粘贴过去即可
STACK SEGMENT STACK
    DW 256 DUP(?)
STACK ENDS

DATA SEGMENT

DATA ENDS

CODE SEGMENT
    ASSUME CS:CODE,DS:DATA,SS:STACK

START:
    MOV AX,DATA
    MOV DS,AX
    XOR AX,AX



    MOV AH,4CH
    INT 21H
CODE ENDS
END START