DATA SEGMENT
STRING DB "1+2="
    BUFF DB ?
    DB "$"
DATA ENDS

CODE SEGMENT
    ASSUME CS:CODE,DS:DATA

START:
    MOV AX,DATA
    MOV DS,AX
    XOR AX,AX

    MOV AX,1
    ADD AX,2

    ADD AX,'0'
    MOV BUFF,AL
    MOV DX,OFFSET STRING
    MOV AH,09H
    INT 21H

    MOV AH,4CH
    INT 21H
CODE ENDS
END START